name: Java CI

on: [push, pull_request]

jobs:
  build:
    name: Build KG Parser
    runs-on: ubuntu-latest
    steps:
      - run: mysql --version
      - run: systemct start mysql
      - run: sudo mysql -e "create database actions;"
